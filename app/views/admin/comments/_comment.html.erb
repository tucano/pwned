<blockquote class="comment">
    <p><%= simple_format auto_link(h(comment.body)) %></p>
    <p> <%= link_to 'Show', [ :admin, @network, comment]%> | 
        <%= link_to 'Edit', [:edit, :admin, @network, comment] %> | 
        <%= link_to 'Destroy', [:admin, @network, comment], :confirm => 'Are you sure?', :method => :delete %>
    </p>
</blockquote>