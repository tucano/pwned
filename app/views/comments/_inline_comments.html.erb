<h4 class="comment">
  <% if @comments.empty? %>
  New comment
  <% else %>
  Comments (last <%= @comments.size %>)
  <% end %>
</h4>

<%= render :partial => 'comment', :collection => @comments %>


<blockquote>
  <%= render :partial => 'inline_new' %>
</blockquote>