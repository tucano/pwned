<% content_for(:style) do %>
  <%= stylesheet_link_tag 'mf54_reset' %>
	<%= stylesheet_link_tag 'mf54_grid' %>
  <%= stylesheet_link_tag 'mf54_content' %>
	<%= stylesheet_link_tag 'pwned' %>
<% end %>

<% content_for(:toolbox) do %>
  <li><%= link_to 'Show', @comment.network %></li>
  <li><%= link_to 'Edit', edit_network_path(@comment.network) %></li>
	<li><%= link_to 'Destroy', @comment.network, :confirm => 'Are you sure?', :method => :delete %></li>
<% end %>

<% content_for(:navbar) do %>
  <%= render :partial => 'shared/menu_user' %>
  <%= render :partial => 'shared/menu_toolbox' %>
  <%= render :partial => 'shared/menu_networks' %>
  <%= render :partial => 'shared/menu_help' %>
<% end %>

<!-- PAGE TITLE -->
<h1 class="pagetitle">Editing comment for <%= @comment.network.name %></h1>

<div id="notice">
	<p style="color: green"><%= flash[:notice] %></p>
</div>

<!-- C.1 CONTENT -->
<div class="content">
  <!-- LIST CELL -->
	<div class="corner-content-1col-top"></div>
	<div class="content-1col-nobox">
    <h1>Editing comment</h1>
    <% form_for(@comment) do |f| %>
      <%= f.error_messages %>
      <p>
        <%= f.label :body %><br />
        <%= f.text_area :body %>
      </p>
      <p>
        <%= f.submit 'Update' %>
      </p>
    <% end %>
    <%= link_to 'Back', network_comments_path(@network) %>
  </div>
  <div class="corner-content-1col-bottom"></div>
</div>
<!-- C.2 SUBCONTENT -->
<div class="subcontent">
	<%= render(:partial => 'shared/login') %>
	<%= render(:partial => 'shared/search') %>	
	<%= render(:partial => 'shared/news') %>	
</div>